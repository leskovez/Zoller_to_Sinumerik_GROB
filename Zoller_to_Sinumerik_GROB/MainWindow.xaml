<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Zoller_to_Sinumerik_GROB.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Zoller_to_Sinumerik_GROB"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Zoller_to_Sinumerik_GROB">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="InputBox"
                 PlaceholderText="Scan Zidcode tag"
                 Grid.Row="0"/>
        <TextBlock x:Name="OutputText"
                   Visibility="Visible"
                   Grid.Row="1"/>
        <TextBlock x:Name="ConvertedText"
                   Visibility="Visible"
                   Grid.Row="2"/>
        <Button Content="Translate to tool length"
                Click="transButtonClick"
                Grid.Row="3"/>
        <Button Content="Write to GROB tool data file"
                Click="writeButtonClick"
                Grid.Row="4"/>
    </Grid>
</Window>
